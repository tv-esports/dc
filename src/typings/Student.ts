export type StudentType = {
    discord_id: string;
    email: Array<string>;
    customer_id: string;
    offers: Array<string>;
    roles: Array<string>;
    inserted_at: Date;
    updated_at: Date;
}